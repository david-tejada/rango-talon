tag: browser
and tag: user.rango_keyboard

-

# TODO: Not sure if there is a clean way to get all the
# possible hint combinations without enumerating them all in this file
key(a):
   user.rango_click_hint('a')

key(s):
    user.rango_click_hint('s')

key(d):
    user.rango_click_hint("d")